# exponera ha mcp publikt

- aktivera mcp server i home assistant  
- sätt upp https och auth  
- använd cloudflare tunnel/ngrok för publik url  
- notera mcp endpoint‑url och token  
- sätt `MCP_PUBLIC_URL` och token i firmware