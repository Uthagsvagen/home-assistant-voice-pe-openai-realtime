#include <stdbool.h>
#include <stdint.h>
#include <stddef.h>

void wakeword_init() {
    // initialize wake word detection
}

bool wakeword_detected(const int16_t* buf, size_t frames) {
    return false;
}